import LandingPage from "./pages/LandingPage";

// App component is now only used for the main landing page
// All routing is handled by Next.js App Router
function App() {
  return <LandingPage />;
}

export default App;
